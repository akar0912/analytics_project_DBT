version: 2

seeds:
  - name: staking_Rewards
    config:
      column_types: 
        source_ts_ms: BIGINT
        amount: FLOAT
        xx: REAL
  - name: staking_Rates
    config:
      column_types: 
        source_ts_ms: BIGINT